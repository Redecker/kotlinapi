spring:
  jpa:
    hibernate:
      ddl-auto: update
  datasource:
    url: jdbc:mysql://localhost:3307/trivagocase?createDatabaseIfNotExist=true
    username: admin
    password: root
  cache:
    type: simple